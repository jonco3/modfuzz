modfuzz
=======

This is a attempt to write a simple fuzzer to test the DOM module loader.

It generates random script/module graphs and serves them with a node based
webserver.

Start the server with:

 $ node server.mjs

Then browse to http://localhost:8000/ to access the fuzzer.
